import { createBrowserRouter } from "react-router-dom";

const publicRouter = createBrowserRouter([
	{
		path: "/",
		element: <div>OJT Readiness and Risk Prediction System</div>,
	},
]);

export default publicRouter;
